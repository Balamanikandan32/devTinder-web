// export const BASE_URL = "http://localhost:3000";

// For now changing the BASE_URL TO /api, once learn about env we nnned to  modify this BASE_URL

export const BASE_URL = "/api/"; // /api/ means it  take the frontend domain name eg 123.43.45/api/ as base_url
